<?php
class BanksController extends CMS_Controller {
    public function run() {
        $this->model()->initValues(array('currencies', 'country_id'));
        $this->page->sLink = '/admin/banks/';
        parent::run();
    }
}